# راهنمای تست و عیب‌یابی سیستم

## 🔍 تست سیستم

برای بررسی اینکه آیا همه فایل‌ها در کنار هم درست کار می‌کنند، از اسکریپت تست استفاده کنید:

```bash
python test_system.py
```

### چه چیزهایی بررسی می‌شود؟

1. **ساختار فایل‌ها** - بررسی وجود تمام فایل‌های لازم
2. **Importها** - بررسی اینکه همه ماژول‌ها قابل import هستند
3. **تنظیمات** - بررسی config و مقادیر .env
4. **دیتابیس** - بررسی اتصال و ساختار دیتابیس
5. **Handlerها** - بررسی تمام handlerها
6. **ابزارها** - بررسی utility modules
7. **ماژول‌های هسته** - بررسی core modules

### نتایج تست

- ✅ **موفق**: همه چیز درست است
- ⚠️ **هشدار**: مشکلی نیست اما بهتر است بررسی شود
- ❌ **خطا**: مشکل بحرانی که باید برطرف شود

### مثال خروجی

```
✅ موفق (49):
  ✅ bot.py
  ✅ config.py
  ...

⚠️ هشدارها (11):
  ⚠️ فایل اختیاری یافت نشد: .env
  ⚠️ API_ID تنظیم نشده

❌ خطاها (3):
  ❌ Import خطا: aiohttp - No module named 'aiohttp'
```

## 🛠️ برطرف کردن خطاها

### خطای Import (مثل aiohttp)

```bash
pip install -r requirements.txt
```

### خطای تنظیمات (.env)

1. فایل `.env` را ایجاد کنید
2. از پنل تنظیمات ربات استفاده کنید: `/start` → تنظیمات → تنظیمات .env
3. یا به صورت دستی فایل `.env` را ایجاد و پر کنید

### خطای دیتابیس

دیتابیس به صورت خودکار در اولین اجرا ایجاد می‌شود. اگر خطا دارید:
- بررسی کنید که دایرکتوری `database/` وجود دارد
- بررسی کنید که دسترسی نوشتن دارید

## 📦 بررسی وابستگی‌ها

برای بررسی سریع وابستگی‌های نصب شده:

```bash
python check_dependencies.py
```

این اسکریپت نشان می‌دهد کدام پکیج‌ها نصب شده و کدام‌ها نیست.

## ✅ چک‌لیست قبل از اجرا

- [ ] همه وابستگی‌ها نصب شده: `pip install -r requirements.txt`
- [ ] فایل `.env` ایجاد و تنظیم شده
- [ ] تست سیستم اجرا شده: `python test_system.py`
- [ ] هیچ خطای بحرانی وجود ندارد
- [ ] دیتابیس قابل دسترسی است

## 🚀 بعد از برطرف کردن خطاها

پس از اینکه همه خطاها برطرف شد:

```bash
python bot.py
```

ربات باید بدون خطا راه‌اندازی شود.

## 📝 نکات

- **هشدارها** معمولاً مشکلی ایجاد نمی‌کنند
- **خطاهای بحرانی** باید برطرف شوند
- اگر `.env` ندارید، از پنل تنظیمات ربات استفاده کنید
- همیشه قبل از اجرای ربات، تست را اجرا کنید


