# راهنمای استفاده از پنل تنظیمات .env

## 📋 مقدمه

پنل مدیریت تنظیمات .env به شما امکان می‌دهد تمام تنظیمات ربات را از طریق رابط تلگرام مدیریت کنید، بدون نیاز به دسترسی مستقیم به فایل .env

## 🚀 نحوه دسترسی

1. ربات را با `/start` راه‌اندازی کنید
2. به منوی **⚙️ تنظیمات** بروید
3. گزینه **⚙️ تنظیمات .env** را انتخاب کنید

## 📱 ساختار منو

### منوی اصلی تنظیمات .env

- **📱 تنظیمات Telegram** - API_ID, API_HASH, BOT_TOKEN
- **👥 گروه‌ها و کانال‌ها** - SOURCE_GROUP_ID, STORAGE_GROUP_ID, TARGET_CHANNEL_ID, ADMIN_USER_ID
- **🤖 تنظیمات OpenRouter** - OPENROUTER_API_KEY, OPENROUTER_MODEL
- **💾 تنظیمات دیتابیس** - DB_PATH
- **🌐 سایر تنظیمات** - TIMEZONE
- **📋 نمایش همه** - نمایش تمام تنظیمات در یک صفحه

## ✏️ نحوه ویرایش

1. دسته‌بندی مورد نظر را انتخاب کنید
2. متغیر مورد نظر را انتخاب کنید
3. مقدار جدید را ارسال کنید
4. برای لغو، `/cancel` را ارسال کنید

### مثال:

```
1. تنظیمات .env → تنظیمات Telegram → API_ID
2. مقدار جدید را ارسال کنید: 12345678
3. ✅ ذخیره می‌شود
```

## 🔒 امنیت

- **مقادیر حساس** (API_HASH, BOT_TOKEN, OPENROUTER_API_KEY) به صورت ماسک شده نمایش داده می‌شوند
- فقط **4 کاراکتر اول و آخر** نمایش داده می‌شود
- مثال: `1234****5678`

## ✅ اعتبارسنجی

سیستم به صورت خودکار مقادیر را اعتبارسنجی می‌کند:

- **API_ID**: باید عدد باشد
- **Group/Channel IDs**: باید عدد باشند
- **DB_PATH**: نمی‌تواند خالی باشد
- **OPENROUTER_MODEL**: باید با `google/gemini-` شروع شود

## ⚠️ نکات مهم

1. **راه‌اندازی مجدد**: پس از تغییر تنظیمات حساس (مثل API keys)، ربات را راه‌اندازی مجدد کنید
2. **پشتیبان‌گیری**: قبل از تغییرات مهم، از فایل .env پشتیبان بگیرید
3. **تست**: پس از تغییرات، ربات را تست کنید تا مطمئن شوید همه چیز درست کار می‌کند

## 🛠️ عیب‌یابی

### مشکل: تغییرات اعمال نمی‌شود
- ربات را راه‌اندازی مجدد کنید
- بررسی کنید که فایل .env قابل نوشتن است

### مشکل: خطای اعتبارسنجی
- فرمت مقدار را بررسی کنید
- برای IDs، فقط عدد ارسال کنید
- برای مدل OpenRouter، فرمت صحیح را استفاده کنید

### مشکل: مقادیر حساس نمایش داده نمی‌شود
- این یک ویژگی امنیتی است
- برای تغییر، مقدار کامل را دوباره ارسال کنید

## 📝 مثال‌های استفاده

### تغییر API_ID
```
منو → تنظیمات → تنظیمات .env → تنظیمات Telegram → API_ID
ارسال: 12345678
```

### تغییر مدل OpenRouter
```
منو → تنظیمات → تنظیمات .env → تنظیمات OpenRouter → OPENROUTER_MODEL
ارسال: google/gemini-2.5-flash:free
```

### تغییر Group ID
```
منو → تنظیمات → تنظیمات .env → گروه‌ها و کانال‌ها → SOURCE_GROUP_ID
ارسال: -1001234567890
```

## 🔄 راه‌اندازی مجدد ربات

پس از تغییر تنظیمات مهم:

1. ربات را متوقف کنید (Ctrl+C)
2. دوباره راه‌اندازی کنید: `python bot.py`

یا اگر از systemd استفاده می‌کنید:
```bash
sudo systemctl restart ketabrooz-bot
```


