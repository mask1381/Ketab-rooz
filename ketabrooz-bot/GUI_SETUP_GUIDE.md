# راهنمای استفاده از GUI تنظیمات

## 🖥️ پنجره گرافیکی تنظیمات .env

یک رابط گرافیکی ساده برای وارد کردن و ذخیره تنظیمات ربات.

## 🚀 نحوه اجرا

### روش 1: اجرای مستقیم
```bash
python setup_env_gui.py
```

### روش 2: استفاده از launcher
```bash
python run_setup.py
```

## 📋 بخش‌های مختلف

### 📱 تنظیمات Telegram

- **API_ID**: از [my.telegram.org](https://my.telegram.org) دریافت کنید
- **API_HASH**: از [my.telegram.org](https://my.telegram.org) دریافت کنید  
- **BOT_TOKEN**: از [@BotFather](https://t.me/BotFather) دریافت کنید

### 👥 گروه‌ها و کانال‌ها

- **SOURCE_GROUP_ID**: ID گروهی که PDFها در آن آپلود می‌شوند
  - مثال: `-1001234567890`
  - برای دریافت ID: از ربات @userinfobot استفاده کنید
  
- **STORAGE_GROUP_ID**: ID گروه ذخیره‌سازی داخلی
  - مثال: `-1009876543210`
  
- **TARGET_CHANNEL_ID**: ID کانال عمومی برای انتشار
  - مثال: `-1005555555555`
  
- **ADMIN_USER_ID**: ID کاربری شما (عدد مثبت)
  - مثال: `123456789`
  - برای دریافت: از ربات @userinfobot استفاده کنید

### 🤖 تنظیمات OpenRouter

- **OPENROUTER_API_KEY**: کلید API از [openrouter.ai](https://openrouter.ai)
- **OPENROUTER_MODEL**: مدل AI
  - پیشنهادی: `google/gemini-2.5-flash:free`
  - گزینه‌های دیگر: `google/gemini-2.0-flash-exp:free`

### 💾 تنظیمات دیتابیس و سایر

- **DB_PATH**: مسیر فایل دیتابیس
  - پیش‌فرض: `database/ketabrooz.db`
  
- **TIMEZONE**: منطقه زمانی
  - پیش‌فرض: `Asia/Tehran`

## 🔧 دکمه‌ها

### 💾 ذخیره تنظیمات
- تمام تنظیمات را در فایل `.env` ذخیره می‌کند
- قبل از ذخیره، اعتبارسنجی انجام می‌شود

### 📂 بارگذاری از فایل
- اگر فایل `.env` موجود باشد، مقادیر را بارگذاری می‌کند

### ✅ تست تنظیمات
- بررسی می‌کند که آیا تنظیمات معتبر هستند
- تلاش می‌کند config را بارگذاری کند

### ❌ خروج
- پنجره را می‌بندد

## ⚠️ نکات مهم

1. **مقادیر حساس**: API_HASH، BOT_TOKEN و OPENROUTER_API_KEY به صورت `***` نمایش داده می‌شوند
2. **اعتبارسنجی**: قبل از ذخیره، تمام فیلدها بررسی می‌شوند
3. **پشتیبان‌گیری**: قبل از تغییرات، از فایل `.env` پشتیبان بگیرید

## 🔍 دریافت IDها

### دریافت User ID
1. به ربات [@userinfobot](https://t.me/userinfobot) پیام دهید
2. ID شما نمایش داده می‌شود

### دریافت Group/Channel ID
1. ربات را به گروه/کانال اضافه کنید
2. از ربات [@userinfobot](https://t.me/userinfobot) استفاده کنید
3. یا از ربات [@getidsbot](https://t.me/getidsbot) استفاده کنید

## ✅ بعد از ذخیره

پس از ذخیره تنظیمات:

1. فایل `.env` در دایرکتوری `ketabrooz-bot` ایجاد/به‌روزرسانی می‌شود
2. می‌توانید ربات را راه‌اندازی کنید:
   ```bash
   python bot.py
   ```

## 🐛 عیب‌یابی

### GUI باز نمی‌شود
- بررسی کنید که tkinter نصب است: `python -m tkinter`
- در Windows معمولاً با Python نصب می‌شود

### خطای اعتبارسنجی
- بررسی کنید که همه فیلدهای الزامی پر شده‌اند
- فرمت IDها را بررسی کنید (عدد باشند)

### تنظیمات ذخیره نمی‌شود
- بررسی کنید که دسترسی نوشتن در دایرکتوری دارید
- بررسی کنید که فایل `.env` قفل نشده باشد


